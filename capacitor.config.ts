import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'be.thomasmore.graduaten.pokedraw',
  appName: 'pokedraw',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
